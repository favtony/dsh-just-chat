# dsh-just-chat 插件规格

## 目标

`dsh-just-chat` 为 DSH Web UI 增加“`不在项目中工作`”模式。用户在新建对话的工作区选择器中选中该模式后，不创建目录、不创建会话；选择框文案变为“`不在项目中工作`”，用户可输入第一条消息。仅在用户提交第一条消息时，插件根据设置中的对话根目录和目录名模板创建目录、创建会话、发送这条消息。

界面必须直接复用当前 DSH 安装中已经注册并由 renderer 实际使用的官方 live slot entry。工作区下拉框、左侧工作区浏览器和输入栏不能由插件复制、重画或降级；插件只在这些 live entry 的数据和回调边界上增加只聊天模式、自动对话投影和首条消息交接。内部组件缺失、组件形状不符或宿主版本不匹配时，安装或启动直接失败，不显示替代界面。

左侧导航由两个官方 `WorkspaceBrowser` 实例纵向组成：“工作区”实例只接收真实工作区和普通会话，固定按工作区分组；“对话”实例不接收工作区，只接收由 `dsh-just-chat` 创建的顶层会话，固定使用官方 flat 模式，因此“对话”与“工作区”处于同一顶层。两个实例的官方行、菜单、拖动、搜索、弹窗和布局继续由宿主组件渲染；第二个实例保留官方固定的搜索框和视图菜单，没有公开 props 可以移除它们。对话初始按创建时间倒序排列，独立使用插件保存的手工顺序。

## 已确定的范围

- 插件名为 `dsh-just-chat`。
- “`不在项目中工作`”作为官方工作区条目列表的最后一项出现在新建对话的工作区下拉菜单中；官方“添加工作区”固定 footer 仍由宿主渲染在其后。公开 props 没有 footer 顺序控制，插件不复制菜单去改变这一顺序。
- 选择该项只切换新建对话的待选模式，不产生磁盘或会话副作用。
- 用户提交第一条消息时才创建目录和会话。
- 对话根目录与目录名模板注册到 DSH 既有设置页的“插件 -> 插件配置”卡片，不使用已禁用的“默认目录”功能。
- 自动目录不登记为普通工作区，也不进入“工作区”列表。
- “对话”列表初始按插件记录的 `createdAt` 倒序；用户拖动后的浏览器本地顺序优先，时间相同按 `sessionId` 升序稳定排序。
- “对话”列表有独立搜索按钮，搜索标题和消息内容。
- 本版本不提供自动对话与普通工作区会话的互相转换。
- 本版本不提供删除会话或删除目录；删除能力列为后续迭代。

## 非目标

- 不创建真正没有 `cwd` 的会话。提交首条消息后仍必须创建真实目录，作为会话的 `cwd`。
- 不修改手动工作区的选择、添加、分组、排序、拖动、归档、标题、模型选择、预设选择或会话树行为。
- 不复用或恢复已禁用的“默认目录”插件。
- 不允许模板执行任意 JavaScript。
- 不自动清理创建失败留下的目录，不做目录配额、跨机器同步或旧会话迁移。

## 已核实的宿主约束

当前实现基线固定为官方 `dsh-v0.1.1-rc.2`（提交 `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`）；该提交同时是获取时的官方 `master`，没有更晚的未发布提交。以下是该版本已确认的能力和限制：

- 设置系统允许功能插件注册独立 `settings.section` 分区，也提供由 `ui-settings-plugins` 统一承载的 `settings.plugins.tab` 与 `settings.plugin.item`；插件配置卡片以自己的设置命名空间作为 keyed slot 的 `key`，由官方“插件 -> 插件配置”页按宿主实际提供的命名空间统一渲染。`settings.onboarding` 的公开 owner 新增 `openSection(id)`，因此缺失根目录时可以由插件的引导项正规打开插件配置所在的 `plugins` 设置分区，不再需要私有接口或 DOM 操作。
- 官方 `session.create` 支持预分配 `sessionId` 与 `cwd`，并会创建真实会话和空闲 agent。客户端通过公开 `ctx.connection.api.sessions.create({ cwd, sessionId })` 调用；RPC 成功后 runtime manager 会立即把该会话同步进 `ctx.sessions.list`，可安全打开并交接草稿。`ctx.sessions` 自身是收窄的读取、搜索和打开服务，不提供 `create`。
- 会话列表提供 `cwd`、`updatedAt`、`displayTitle`、`blank`、运行状态、父会话关系和归档投影，足以做分类、排序和展示；官方 `session.search` 可搜索消息内容。
- `conversation.hero.workspace`、`conversation.composer.bar` 和 `sidebar.workspaces` 均是单一占位。宿主 renderer 的 `ctx.slots.entries()` 返回这些占位当前实际使用的 live entry；插件只能保存其原始 `component`，在严格契约检查后把同一 entry 的 `component` 替换为适配器。适配器必须调用保存的官方组件，并继续使用该 entry 原有的 `inject`、`children`、store、locale 和 renderer 标准 props。
- 官方仍没有公开的“未创建会话草稿控制器”或无会话提交钩子，也没有可供外部插件读取已暂存的新建会话预设的服务。`0.1.1-rc.2` 新增的 `conversation.input.attachments` 只负责附件展示；因此只聊天适配器只改变官方 `InputBar` 的输入源、提交回调和必要的无会话可编辑状态，模型、预设、附件、命令和计划仍由官方 entry 的原始 props 决定。真实会话建立并完成草稿交接后，适配器立即透传官方输入栏。
- 不允许复制官方组件源码、重新注册同名低优先级 entry、禁用 `ui-workspace`，也不允许任何“简化 UI”或行为不等价的 fallback。live entry 读取、形状校验、组件替换和卸载恢复集中在一个适配模块中。
- 官方 `InputActions` 会在真实会话建立后向会话作用域提供 `setDraft` 和 `submit`；插件可用它将首条文本交回标准输入状态机，而不是自行调用模型或伪造会话事件。

前一版规格中“选择后立即创建目录和会话”“仅接管两个 UI 占位”和“宿主端直接创建会话”的表述已删除，不能作为实现依据。附件、模型和其他控件保持官方无会话状态原有规则。

## 核心对象与状态

### 新建页待选模式

新建页只有本地状态，不创建 DSH 会话：

- `未选择`：沿用现有“请选择工作区”行为，输入栏不可提交。
- `普通工作区(workspaceId)`：沿用现有行为，连接工作区既有或新建的空白会话。
- `不在项目中工作`：文本草稿可编辑，待选模式和文本草稿持久化在浏览器本地存储，尚无会话和目录；其余控件的状态保持官方无会话输入栏原有规则。
- `正在创建`：仅发生在首条消息提交后，输入、模式选择和提交按钮都不可重复操作。
- `交接到会话`：目录和会话已完成，插件正在把草稿交给官方输入状态机；该阶段不再创建第二个目录或会话。

在“`不在项目中工作`”模式下重新打开选择器，可以改选普通工作区或重新选择该模式。改选普通工作区时，插件创建或打开该工作区的官方空白会话，并把文本草稿写入该会话的官方输入状态机，不自动发送；改回“`不在项目中工作`”时保留草稿。浏览器关闭、重新打开、刷新或点击侧边栏“新建对话”后，恢复“不在项目中工作”待选模式和未发送文本；侧边栏操作可能按宿主原有规则打开普通工作区空白会话，但插件不清空草稿，也不把它自动写入该会话。附件、模型和其他控件均继续遵循宿主无会话状态的规则。

### 自动对话目录

“自动对话目录”是对话根目录内、仅服务一个顶层会话的真实目录。提交首条消息后，它成为会话 `cwd`。目录相对路径由目录名模板渲染，允许多级子目录。

状态：未渲染 -> 渲染并验证模板 -> 创建目录树和 prepared 记录 -> 官方创建会话 -> 激活分类记录 -> 无预设直接交接，或预设事件与三秒等待竞争 -> 交接首条消息 -> 已发送或等待用户重试。

目录创建成功而官方会话创建失败时，保留空目录和 `prepared` 记录；不宣称会话已创建，也不发布为活动自动对话或失败条目。插件不静默重试。

### 自动对话会话记录

插件持久化每个自动对话的分类记录，最小字段：

- `sessionId`：会话身份。
- `cwd`：创建后的规范化目录路径。
- `rootDirectory`：创建时的规范化根目录。
- `createdAt`：记录创建时间，用于“对话”列表的初始倒序和时间并列时的辅助信息。
- `template`：创建时实际使用的目录模板，用于诊断，后续设置变更不回写。

分类权威来自该记录，不能用“`cwd` 是否位于根目录下”推断。根目录变更、用户手动移动目录、符号链接、普通会话位于同一路径树，都不能改变会话归属。

记录需要有“会话已创建、分类尚未写完”的可恢复标记。插件启动时按 `sessionId` 和 `cwd` 查询真实会话：`prepared` 会话存在且目录一致时激活记录，`active` 会话存在且目录一致时保留记录，不能让有效会话永久消失于“对话”列表；任一状态的会话不存在时删除记录、保留空目录且不在“对话”列表显示任何条目。

### 首条消息交接

“`不在项目中工作`”模式下暂存用户输入的文本草稿。会话创建成功后，无预设部署可直接打开该会话；存在预设的部署则在创建前监听该会话的 `agent-preset/selected`，创建成功后开始三秒等待。事件先到时立即交接草稿；三秒先到时按当时会话实际预设交接草稿。随后插件通过官方 `InputActions.setDraft` 和 `InputActions.submit` 交给既有输入状态机，保证首条消息只走一次常规投递路径。

如果目录和会话创建成功、但首条消息投递失败，新会话保留已交接草稿，用户在该会话中按普通会话方式再次提交。创建过程中关闭或刷新浏览器时，未交接的“不在项目中工作”文本草稿和待选模式从浏览器本地存储恢复；已创建的会话和分类记录仍会恢复。草稿在成功写入真实会话的官方输入状态机后清除本地副本。

## 设置页

插件在 DSH 既有设置面板的“插件 -> 插件配置”页注册一个以 `dsh-just-chat` 设置命名空间为 key 的官方插件配置卡片。卡片标题和说明使用“`对话目录`”，准确说明该卡片只配置自动对话的根目录和目录名模板；新建页菜单仍使用“`不在项目中工作`”。

### 设置页原生布局与根目录浏览

本次实现沿用 DSH 当前内部产物的设置页行式布局和公开原生控件：每个设置项使用官方设置行的左侧标题/说明、右侧控件和底部分隔线；文本框使用 `@deepseek-ai/dsh-client-ui-primitives` 的 `Input`，操作使用同包的 `Button` 与官方浏览图标，不再把标签、输入框和按钮挤在同一行，也不在分区内容里绘制额外的关闭按钮。

“对话根目录”一行保留可编辑路径输入，并增加“浏览”按钮。按钮只调用当前 DSH 客户端运行时公开的 `ctx.workspaces.pickDirectory()`，由 Host 原生 `host.pickDirectory` 流程选择现有目录；取消选择不改变输入值，选择成功后仅回填表单值，仍由现有“保存”操作负责校验和持久化。根目录控件和目录名模板控件各自占卡片内容中的独立一行；卡片内部使用公开 DSH primitives，不复制官方卡片或设置外壳。模板输入、预览、保存、错误展示和缺少根目录引导逻辑不改变。

### 对话根目录

- 使用既有目录选择器选择宿主可访问的现有目录。
- 保存时解析为规范化绝对路径，并验证它存在且是目录。
- 不自动创建根目录。
- 根目录变更只影响之后提交的自动对话，不移动已有目录，不改变已有会话归属。
- 未配置根目录时仍可以在新建页选择“`不在项目中工作`”并输入草稿；首次提交会打开此设置分区、保留草稿且不创建任何目录或会话。保存有效设置后，用户手动再次提交，插件不替用户自动发送。

### 目录名模板

默认模板为：

`${date.yyyy}-${date.MM}-${date.dd}/${time.HH}-${time.mm}-${time.ss}-${message.firstSentence}`

其结果等同于“日期/时间-第一句”。模板使用 JavaScript 模板字符串的插值外形 `${...}`，但只接受下表中的受限参数；不会执行用户输入的 JavaScript、属性访问、运算符、数组下标或任意函数。

| 参数 | 含义 |
| --- | --- |
| `${date.yyyy}` | 主机本地时区的四位年份。 |
| `${date.MM}` | 两位月份。 |
| `${date.dd}` | 两位日期。 |
| `${time.HH}` | 两位小时，24 小时制。 |
| `${time.mm}` | 两位分钟。 |
| `${time.ss}` | 两位秒。 |
| `${message.firstSentence}` | 消息按英文逗号、中文逗号、英文句号、中文句号分割后的第一个非空片段；删除全部空白字符后生成安全的路径文字。 |
| `${message.words(n)}` | 原消息中从左至右取得前 `n` 个英文单词，`n` 必须是 1 到 32 的十进制整数；英文单词使用 ASCII 字母组成，可在内部包含一个撇号。 |

模板解析和路径规则：

1. `/` 是唯一允许的路径分隔符，一个 `/` 创建一级子目录。例如 `${date.yyyy}/${date.MM}/${message.words(3)}` 创建年、月、消息词组三级目录。
2. `\` 在模板中一律非法，不交给 JavaScript 或 Node 路径工具解释。这消除 Windows 路径、转义和跨平台行为的歧义。
3. 模板始终是相对根目录的路径，渲染结果由宿主追加到已配置的对话根目录；模板不支持 `${cwd}`。根目录已由设置单独决定，在模板中再次写入既多余又会混淆相对路径和绝对路径。
4. 任何解析结果都必须是根目录的严格后代。绝对路径、盘符、UNC 路径、空段、`.`、`..`、重复分隔符、前导或尾随 `/` 都无效。
5. 静态文字和动态参数在每一级目录名中都必须满足 Windows 文件名规则：不能含控制字符或 `< > : " / \ | ? *`，不能以空格或句点结尾，不能是 Windows 保留名，且单级目录名不超过 80 个 Unicode 码点。
6. 消息派生参数会先进行 Unicode 规范化，删除空白和控制字符，再删除 Windows 非法字符；每个派生参数最多保留 48 个 Unicode 码点。该转换是参数语义的一部分，不是错误后的替代命名。
7. 保存设置时校验模板语法、参数、静态路径段和以示例消息渲染的结果。提交首条消息时再次按真实消息校验；真实消息渲染出空目录名或超长路径时拒绝提交，保留草稿并显示具体模板错误。
8. 模板必须含有至少一个能形成叶目录名的静态文字或时间参数；不能仅由可能为空的消息参数构成。

设置页在模板输入框下方显示参数清单、默认模板和基于固定示例消息的只读路径预览。预览只验证格式，不替代提交时的最终校验。

## 交互流程

### 选择“不在项目中工作”

1. 用户打开新建对话页的工作区下拉菜单。
2. 在官方工作区条目列表末尾的“`不在项目中工作`”项点击；“添加工作区”仍使用官方 footer。
3. 菜单关闭，选择框显示“`不在项目中工作`”，该项带选中状态。
4. 新建页输入栏只解除原本无会话状态下的文本编辑和发送限制；模型、预设、附件、命令、计划和其他控件均保持宿主无会话输入栏原有的显示、可用性和行为，不由本插件额外禁用、替换或简化。
5. 此时不验证根目录、不渲染模板、不创建目录、不创建会话。

### 提交第一条消息

1. 用户点击发送或按现有提交快捷键。
2. 插件冻结文本草稿与提交模式，输入栏进入“正在创建”状态，阻止重复提交。
3. 客户端请求宿主 `POST /api/dsh-just-chat/preparations`。宿主使用当前设置和冻结草稿渲染目录模板，验证根目录和最终路径，生成 UUID 形式的 `sessionId`，创建目录树，并持久化 `prepared` 记录，返回 `sessionId` 和 `cwd`。
4. 客户端调用公开 `ctx.connection.api.sessions.create({ cwd, sessionId })`，检查 RPC 成功结果。这一步由 DSH 自己创建真实会话和空闲 agent，不由插件伪造或直接操作会话内部对象。
5. 客户端请求宿主 `POST /api/dsh-just-chat/preparations/:sessionId/activate`。宿主核对预留记录、会话 ID 与 `cwd` 后，把记录发布为自动对话；之后客户端打开会话。
6. 无预设部署直接进入交接；存在预设的部署在会话创建前订阅该 `sessionId` 的 `agent-preset/selected`，会话创建成功后开始三秒等待。事件先到时立即交接，三秒先到时取消监听并按当时会话实际预设交接。交接组件通过官方 `InputActions.setDraft` 写入文本，并调用 `InputActions.submit`。投递成功后，该会话出现在“对话”列表中，按其插件记录的 `createdAt` 进入初始顺序。

失败规则：

- 根目录或模板无效：不创建目录和会话，保留草稿；缺失根目录时打开本插件设置分区。
- 目录创建失败：不创建会话，保留草稿。
- 官方会话创建失败：`prepared` 记录与已创建空目录保留，不发布失败条目；再次提交使用新的预留 ID，不复用旧目录。
- 激活记录失败：不投递首条消息；记录保持 `prepared`，启动恢复流程会按预分配 `sessionId` 和 `cwd` 重新核对。
- 预设切换在三秒内失败：会话已存在且在“对话”列表中；三秒届满时仍按当时会话实际预设交接首条草稿。
- 预设切换超过三秒：三秒届满时仍按当时会话实际预设交接首条草稿；之后的预设切换沿用官方对已开始会话的处理。
- 首条消息投递失败：会话已存在且在“对话”列表中，草稿已交接到官方输入栏，用户可按普通会话方式再次提交。

### 左侧“对话”列表

1. 左侧浏览区域按顺序渲染“工作区”和“对话”两个列表。
2. “工作区”完整复用现有行为；自动对话会话必须从该列表排除。
3. “对话”仅显示插件记录关联、未归档、非子 agent 的顶层会话。
4. 条目完整复用官方同类会话行的标题、运行状态、待处理交互、完成提醒、相对更新时间、选中样式、菜单和拖动交互；实现只替换数据筛选和分区容器，不另造简化行。
5. 默认顺序按插件分类记录 `createdAt` 倒序。用户拖动后，浏览器本地持久化该手工顺序；之后的会话活动不改变顺序。新创建的自动对话按其创建时间插入初始列表。
6. “对话”标题右侧提供独立搜索按钮。展开后的搜索输入、加载、清空、无结果和结果行复用官方“工作区”搜索体验；消息内容搜索调用现有宿主搜索能力，再以自动对话记录过滤结果。官方组件的标题本地搜索仍直接匹配完整宿主会话列表，公开 props 没有结果筛选边界，因此普通会话可能因标题命中出现在搜索结果中，这一限制不复制官方搜索 UI 解决。
7. 对话列表为空时显示与工作区列表同等级的空状态，不替换整个侧边栏。
8. 归档沿用 DSH 全局归档语义：从“对话”列表隐藏，但不删除目录或插件分类记录。
9. 分类记录指向不存在会话或记录 `cwd` 与会话 `cwd` 不一致时，启动时清除失效记录，保留目录且不显示条目；不把会话猜测性放回“工作区”列表。

## 场景清单

| 场景 | 处理规则 |
| --- | --- |
| 选中后没有发送消息 | 没有目录、会话或分类记录；关闭、重新打开、刷新或点击侧边栏“新建对话”后恢复“不在项目中工作”待选模式和文本草稿。 |
| 未配置根目录 | 允许选择模式和编辑草稿；提交时打开插件设置分区，保留草稿，不自动发送。 |
| 根目录被删除、改为文件或失去权限 | 提交时拒绝；不创建目录和会话，保留草稿。 |
| 模板包含非法参数、反斜杠或路径穿越 | 设置保存失败；旧的有效设置保持不变。 |
| 首条文本为空 | 发送按钮保持宿主无会话状态的判定结果；不能形成有效文本时不创建目录或会话。其他输入控件继续遵循宿主无会话状态的原有规则。 |
| 消息首句含空白、文件名非法字符或过长文本 | 按消息参数的确定性规范化和长度规则生成；最终路径仍须通过校验。 |
| 两次提交发生在同一秒 | 每次目录创建前追加不可见的冲突检测；若最终目录已经存在，本次提交失败并保留草稿，不复用其他会话目录。 |
| 双击发送或多标签页同时提交 | 每个新建页提交只接受一次；不同页面各自创建独立会话和目录。 |
| 目录创建成功、会话创建失败 | 保留空目录和 `prepared` 记录；不发布为活动自动对话或失败条目。 |
| 会话已创建、分类记录中断 | 启动时按 `sessionId` 和 `cwd` 重新核对；会话存在则完成关联，会话不存在则删除创建中记录、保留空目录且不显示条目。 |
| 会话创建成功、首条消息投递失败 | 打开该会话并保留草稿，用户重试；不再创建目录或会话。 |
| 提交过程中刷新浏览器 | 已完成的会话和分类记录恢复；尚未写入真实会话输入状态机的文本草稿和待选模式从浏览器本地存储恢复。 |
| 更改根目录或模板 | 只影响之后的首条消息；已有目录、会话和记录不变。 |
| 自动目录被手动移动或删除 | 会话仍在“对话”列表；打开或运行失败时沿用宿主对缺失 `cwd` 的错误，不重建目录。 |
| 自动目录被加入普通工作区 | 归属仍由插件记录决定，只显示在“对话”。 |
| 普通会话恰好位于对话根目录 | 没有插件记录就不进入“对话”。 |
| 子 agent | 不进入“对话”列表，继续显示在父会话上下文中。 |
| 搜索 | “工作区”和“对话”搜索各自独立，关闭其中一个不影响另一个。 |
| 旧版本数据 | 没有 `dsh-just-chat` 记录的会话保持既有展示位置，不做路径猜测迁移。 |

## 插件架构

插件包含宿主半部和客户端半部。目录渲染、路径验证和创建事务都在宿主侧；客户端只负责待选模式、草稿暂存、设置界面和展示投影。

### 宿主半部

- 注册 `dsh-just-chat` 设置命名空间，字段为根目录和目录名模板。
- 使用官方 `@deepseek-ai/dsh-storage-domain` 与既有 JSON 存储后端保存 `prepared` 和 `active` 自动对话记录；不另造 `~/.dsh` JSON 文件。
- 提供模板解析器、消息参数提取器、Windows 路径校验器和示例预览生成器；这些纯逻辑在同一模块内测试。
- 仅注册设置原子保存、预留、激活、记录列表和模板预览这五类本地 HTTP 路由。设置保存由宿主在一次 `settings.update` 中同时验证并写入根目录和模板，任一字段无效时旧设置整体不变。会话创建始终调用官方 `session.create`，插件路由不直接创建 agent 或写入会话日志。
- 目录模板不由客户端解释；客户端预览只使用宿主返回的验证和预览结果，保证创建时的行为完全一致。
- 启动时处理 `prepared` 记录：按预分配 `sessionId` 与 `cwd` 验证是否已有真实会话；会话存在则激活，会话不存在则删除创建中记录、保留空目录且不产生侧边栏条目。

### 客户端半部

- 在 `settings.plugin.item` 注册“`对话目录`”插件配置卡片，使用公开表单控件和设置命名空间。固定 rc.2 未公开目录选择 client API，因此根目录由用户输入，宿主在保存和预留时执行路径校验；缺失根目录时通过公开 `settings.onboarding` owner 的 `openSection('plugins')` 打开官方插件设置分区。
- 以一个根作用域视图状态存储预留状态、激活状态和对话搜索状态；以独立的持久化待选草稿存储保存“不在项目中工作”待选模式和纯文本草稿。两个存储均由同一次 `apply` 创建并传给三个注册项，不创建模块级单例。持久化待选草稿存储使用官方 `createSnapshotStore(..., { persist: { name } })`，不自行读写 `localStorage`。
- 读取并适配 `conversation`、`conversation.hero.workspace`、`conversation.composer.bar` 和 `sidebar.workspaces` 的当前 live entry。适配器只包装官方 `component`，不重新声明同名 slot；组件卸载时恢复原始 `component`。
- 工作区选择器适配器通过 `useWorkspaces` 投影只聊天伪 workspace，并只拦截该伪项的 `onPick`；官方菜单、Portal、定位、键盘导航、添加工作区流程和错误 Modal 全部由原始 `WorkspacePicker` 负责。
- 输入栏适配器仅在无会话且待选模式为 `just-chat` 时替换输入源和提交动作；仍调用官方 `InputBar`，保留官方 textarea、工具行、附件槽、模型/计划槽、命令面、提示和布局。真实会话或其他待选模式完全透传原始 props。
- 工作区浏览器适配器通过当前 live entry 复用同一个官方 `WorkspaceBrowser` 组件两次：工作区实例过滤自动对话并固定 `groupBy: 'workspace'`；对话实例提供空工作区、自动对话 session 列表并固定 `groupBy: 'flat'`。两个实例的官方 `deriveGroups`/flat 派生、行组件、菜单、拖动、搜索、折叠和布局继续执行；不再创建虚拟 workspace，也不向真实 Host workspace 写入自动对话。
- 使用现有会话列表、归档投影和 `session.search` 驱动展示与搜索；排序使用插件记录的 `createdAt` 和官方浏览器同类的本地拖动顺序，不在客户端建立第二套会话数据源。

## 实施计划

### 包清单与版本

- 根目录是唯一 npm 包 `dsh-just-chat`，不建立多包工作区。根文件为 `package.json`、`tsconfig.json`、`tsconfig.build.json`、`tsdown.config.ts`、`vitest.config.ts`、`cordis.patch.yml`、`README.md`、`.github/workflows/ci.yml` 和锁文件。
- `package.json` 的根导出为宿主入口 `src/index.ts`，`./client` 导出为浏览器入口 `src/client/index.ts`。`dsh.bundle.patch` 指向 `cordis.patch.yml`；内联 `dsh.client` 声明 `platform: web` 和实际浏览器依赖。不能创建或维护非官方需要的独立 `dsh.client` 清单文件。
- `cordis.patch.yml` 保持默认 `ui-workspace` row 启用，只插入一条名为 `dsh-just-chat` 的双端插件行；插件在客户端启动后适配官方 live entry，不通过 slot 优先级遮蔽官方 entry。
- 运行时依赖只列实际导入的独立 DSH 子包及 `@deepseek-ai/cordis`、React；所有 DSH 子包的 peer dependency 与开发依赖精确固定为 `0.1.1-rc.2`。开发安装关闭 pnpm 的传递 peer 自动补齐，只安装逐项声明的直接开发依赖；运行时 peer 仍由 DSH profile 提供，插件不把整套宿主依赖安装进自身依赖图。不声明或依赖根包 `@deepseek-ai/dsh`。仓库不再嵌入 DSH 源码；升级核验以实际安装的 npm 声明和对应官方标签为准，需要阅读源码时在仓库外临时获取。
- `scripts` 至少提供 `typecheck`、`test`、`build`、`pack` 和本地安装 smoke test。CI 只执行可重复的安装、类型检查、单元测试、构建和打包安装 smoke test；真实 Web 端到端验证单独使用隔离 profile 执行。

### 文件与职责

`src/shared/template.ts` 只解析允许的 `${...}` 参数、提取首句和词数、渲染相对目录名；`src/shared/path.ts` 只校验根目录和渲染结果；`src/shared/types.ts` 定义 `PreparationRecord`、`ConversationRecord`、路由请求和响应的 JSON 字段；`src/shared/errors.ts` 定义可向界面展示的稳定错误码。共享层不依赖 React、Cordis 或 Node IO，也不保存用户消息正文。

`src/host/settings.ts` 注册根目录和模板的设置 schema；`src/host/records.ts` 封装 `@deepseek-ai/dsh-storage-domain`，键为 `sessionId`，记录状态仅有 `prepared` 与 `active`；`src/host/preparation.ts` 是唯一能创建目录的模块；`src/host/recovery.ts` 在启动时对 `prepared` 逐条查询会话持久化元数据；`src/host/routes.ts` 解码 HTTP 边界并调用这些服务；`src/index.ts` 组装宿主服务和路由。

`src/client/stores/view-store.ts` 保存不应跨刷新恢复的提交阶段、错误和对话搜索；`src/client/stores/conversation-order-store.ts` 与 `src/client/stores/pending-draft-store.ts` 分别使用官方持久化 store 保存手工 `sessionId[]` 顺序，以及待选模式 `mode: 'none' | 'workspace' | 'just-chat'` 加纯文本 `draft`。`workspace` 只由用户在工作区选择器中明确选择普通工作区时写入，用于把草稿交给随后建立的官方输入状态；侧边栏打开会话或“新建对话”不会写入该模式。`src/client/api.ts` 是浏览器访问五个同源路由的唯一位置。

`src/client/live-entry.ts` 负责查找、校验、替换和恢复官方 live entry；`src/client/WorkspacePickerAdapter.tsx`、`ComposerBarAdapter.tsx` 和 `SidebarBrowserAdapter.tsx` 只包装官方组件并投影插件数据；`src/client/ConversationHandoff.tsx` 继续承担首条消息交接；`src/client/settings/ConversationDirectorySection.tsx` 只承担设置表单；`src/client/index.ts` 只安装适配生命周期、设置分区和根作用域事件，不注册三个被适配的同名 slot。旧的自绘组件不再进入构建入口，文件暂不删除。

`tests/shared/` 覆盖模板和路径；`tests/host/` 覆盖 records、预留、激活、恢复和路由；`tests/client/` 覆盖 store 与组件；`tests/smoke/` 覆盖构建产物能由隔离 profile 装载。测试不复制产品逻辑作为断言。

### 宿主接口与状态

`WebServer` 只提供精确和前缀路由，不支持 `:sessionId` 模板路径。因此宿主只注册一个 `kind: 'prefix'` 的 `/api/dsh-just-chat` 路由，在处理器中严格匹配下列方法和剩余 pathname；未知路径和方法返回 404。

| 方法与路径 | 输入 | 输出 | 写入和不变量 |
| --- | --- | --- | --- |
| `PUT /settings` | `rootDirectory`、`template` | 保存后的完整设置 | 在宿主一次 `settings.update` 中验证并写入两个字段；任一字段失败时不发生部分更新。 |
| `POST /settings/preview` | `template`、`sampleText` | `valid`、预览相对路径或错误码 | 不读写记录或目录；仅供设置页预览。 |
| `POST /preparations` | `text` | `sessionId`、绝对 `cwd`、创建时间 | 读取已保存设置，验证并渲染，目录不存在才创建；随后持久化完整 `prepared` 记录。请求成功前不暴露 ID。 |
| `POST /preparations/{sessionId}/activate` | `cwd` | `active` 记录 | 仅当 `prepared.sessionId` 与 `prepared.cwd` 完全相等时把同一记录改为 `active`；重复调用返回同一活动记录，不创建第二条。 |
| `GET /conversations` | 无 | 仅 `active` 记录 | 先完成启动恢复，再返回；永不返回 `prepared` 或消息正文。 |

路由使用请求体大小上限、`Content-Type: application/json` 检查和每个字段的运行时 JSON 校验。解析后的首条文本只在本次预留调用内用于目录模板，不能进入 records、日志或错误详情。所有记录写入成功后才返回成功响应。

恢复只读取官方 `ctx.sessionPersistence.list()` 返回的 header：存在同一 `sessionId` 且 `cwd` 相等时把 `prepared` 幂等改为 `active`、保留 `active`；会话不存在或 `cwd` 不一致时删除任一状态记录而不删除目录、不显示条目，也不把任何会话猜测性归入“工作区”。

### 客户端创建状态机

1. `pending-draft-store` 的 `mode === 'just-chat'` 且 `draft.trim()` 非空时，`ComposerBar` 才允许提交；点击或快捷键都调用同一个 `startPreparation()` 回调。
2. `view-store.submission` 从 `idle` 变为 `preparing` 并冻结当前文本。持久化草稿不在此时删除，所以目录、网络或会话创建失败后仍可见且可重试。
3. 在调用预留接口前，根作用域通过 `ctx.remote.$on('agent-preset/selected', handler)` 注册一次监听，回调只处理预分配 `sessionId`；监听器由这一笔提交的生命周期清理。
4. 预留成功后状态为 `creatingSession`，调用公开 `ctx.connection.api.sessions.create({ cwd, sessionId })` 并检查 RPC 成功结果。成功时状态为 `activating`，调用激活接口，再调用 `ctx.sessions.open(sessionId)`。插件不调用模型、agent 或宿主内部会话对象。
5. 激活成功后若部署没有预设立即进入交接；否则启动三秒计时。预设事件先到时清除计时器并交接；计时器先到时注销监听并交接；两者共享一次性 gate，因此晚到事件不能再次发送。
6. `ConversationHandoff` 只在真实会话 scope 中取得公开 `InputActions`。它先 `setDraft(frozenText)`，再调用 `submit()`；两个调用都同步接受后才清除持久化只聊天草稿并把本次提交标为完成。不会自行发 RPC 或伪造日志事件。
7. 任何步骤出错都停止在错误状态、释放监听和计时器、保留持久化草稿，并释放本次提交占位，使用户可用同一草稿重新提交；已有目录和会话遵循宿主恢复规则。提交尚未失败或完成时，重复点击、快捷键和事件晚到都不能再进入预留或调用第二次 `submit()`。

### 四个 slot 的实现边界

- `conversation`：适配当前官方 `ConversationRoot` entry。只在待选模式为 `just-chat` 时把当前会话投影到只聊天伪 workspace，并拦截伪 ID 的官方 `selectWorkspace`，因此顶部标题、选中状态和刷新后的恢复都使用同一个官方根组件计算，不触碰真实 Workspace 连接。
- `conversation.hero.workspace`：适配当前官方 entry 的 `component`。在传给官方组件的 `useWorkspaces` snapshot 末尾投影固定伪 workspace；伪项的 `onPick` 只写 `just-chat` 模式并调用官方 `onClose`，普通 workspace 继续走原始 `onPick`。官方 children、`renderSlot`、directory flow、菜单和错误处理不变。
- `conversation.composer.bar`：适配当前官方 entry 的 `component`。非 `just-chat` 或已经由插件创建并正在交接的会话时原样传递所有 props；只聊天待选状态下，即使 DSH 因普通工作区流程自动选中了一个宿主空白会话，也只替换 `useInput`、`inputActions`、`keyboard` 和必要的 `disabled`/提交状态映射，所有 child slot 和官方 chrome 仍由原始 `InputBar` 渲染。目录和会话只在用户点击发送后创建，交接同步接受后恢复原始组件行为。
- `sidebar.workspaces`：适配当前官方 entry 的 `component`，在一个插件外层中渲染两个官方实例。工作区实例的 `useWorkspaces`/`useSessions` 只投影真实工作区和普通会话，视图状态强制为 `workspace`；对话实例的 `useWorkspaces` 返回空列表，`useSessions` 只返回自动顶层会话，视图状态强制为 `flat`，其 flat 顺序通过现有持久化对话顺序 store 接入官方 `__flat_session_order__` 账户。添加工作区 directory flow 只提供给工作区实例；对话实例不提供 Host workspace 写入口。
- 对话初始排序只用 records 的 `createdAt DESC, sessionId ASC`。拖动顺序单独以官方持久化 store 的模式保存 `sessionId[]`，新记录插入未手工排序部分，后续 `updatedAt` 不重排。消息搜索调用公开 `ctx.sessions.search(query, signal)` 后以活动记录过滤；标题本地搜索沿用官方浏览器对完整宿主会话列表的行为，两个搜索框状态互不共享。

### 左侧导航共用滚动区

当前侧栏适配器在一个外层中纵向渲染两份官方 `WorkspaceBrowser`。外层有受限高度，但“工作区”和“对话”分区各使用 `flex: '1 1 0'`，因此各自被压缩为半高；官方浏览器的 `list` 在这个确定高度内使用 `overflow-y: auto`，于是出现两根互不相干的滚动条。

最小改动只在 `SidebarBrowserAdapter.tsx` 的三个 inline style：

1. 保留现有外层的 `flex: 1` 与 `minHeight: 0`，新增 `overflowY: 'auto'`，使它成为侧栏浏览区域唯一的滚动容器。
2. 将“工作区”和“对话”两个分区的 `flex: '1 1 0'` 改为 `flex: '0 0 auto'`，不再把任一分区限制为半高。
3. 不修改两个官方 `WorkspaceBrowser` 实例。分区高度不再受限时，组件内 `overflow-y: auto` 的列表按内容撑开，不会创建本地滚动区；整体超出侧栏可用高度时，由外层统一滚动。
4. 在隔离 Web 页面验证：长工作区列表和长对话列表连续排列，滚动任何一项时两段随同移动，且独立搜索、拖动排序、菜单、弹窗和收起侧栏不回归。该布局行为不适合用 jsdom 断言，保留现有数据层测试即可。

不采用插件侧 CSS 覆盖官方 CSS module 哈希类名、DOM 查询或双滚动区位置同步：这些方案依赖未公开的宿主实现，升级后容易失效，并违反本插件的 live entry 契约。也不需要扩展或修改 DSH 上游。

每个替换或临时覆盖组件的文件头记录官方参考包、参考文件、固定提交和本插件额外职责。升级 DSH 前先对照这些文件；不得通过 DOM 查询、私有注册表或未发布源文件缩短实现。

### 官方 live entry 适配计划

1. `live-entry.ts` 通过 `ctx.slots.entries(key)` 精确寻找官方 entry，验证 `component`、`inject`、`children`、`locale` 和必要 child slot shape；找不到或不匹配立即抛出错误。
2. 每个适配器保存原始 component，在同一 `StoredEntry` 上安装包装函数；不新增同名注册、不改变 priority/id/order、不修改官方 children/store/inject。
3. `conversation` 根适配器和工作区选择器适配器共享同一个伪 workspace ID；根适配器只投影当前待选会话并拦截该 ID，选择器适配器只扩展 workspace snapshot 和 `onPick`，真实 WorkspaceId 始终交给官方回调。
4. `conversation` 根适配器和工作区选择器适配器继续共享只聊天选择项 ID；根适配器只投影当前待选会话，选择器适配器只扩展 workspace snapshot 和 `onPick`，真实 WorkspaceId 始终交给官方回调。
5. 侧栏适配器在一个外层中复用官方浏览器组件两次：工作区实例固定 workspace 分组并排除自动会话；对话实例固定 flat 分组并只接收自动顶层会话。两个实例的 store/actions 适配保持官方 Hook 调用契约，官方 row、菜单、搜索、拖动、折叠和布局继续执行。
6. 输入栏适配器只在明确 `just-chat` 待选状态提供 pending input snapshot 和一次性提交回调；宿主自动创建的空白会话仍视为待选状态，插件创建并拥有的会话进入交接后立即完全透传；普通活动会话和无待选状态完全透传。
7. 适配生命周期由 `ctx.effect`/`slots.subscribe` 管理，entry 被卸载或插件卸载时恢复原始 component；任何适配器初始化失败都让插件启动失败，不注册或显示 fallback。
8. 增加内部宿主版本和 live entry shape 检查；正常安装脚本检查宿主 npm 包精确版本，启动时再次检查实际 entry，`--ignore-scripts` 也不能绕过启动拒绝。

### 实施、测试与交付顺序

1. 先创建包清单、双入口构建、Cordis patch、精确 peer dependency、Vitest 和 CI；运行 `pnpm install --frozen-lockfile`、`pnpm run typecheck`、`pnpm run build`，并将打包产物安装到临时 profile 验证双端入口被发现。
2. 以测试先行完成 shared：每个允许模板参数、未知参数、反斜杠、绝对路径、路径穿越、保留名、空段、超长、中文首句、英文 words(n)、同秒冲突都有一条独立断言；路径模块没有磁盘副作用。
3. 实现 host records、设置、预留、激活、恢复和单 prefix 路由。测试使用临时根目录和真实 JSON storage backend，断言目录创建顺序、同 ID 激活幂等、`prepared` 不被 `GET /conversations` 发布、`active` 与 `prepared` 的恢复保留/删除规则、路由不持久化文本。
4. 实现 settings 分区与预览，断言保存无效设置不覆盖最后一次有效设置；未配置根目录时首条提交只打开设置并保留草稿。
5. 实现两份客户端 store 和 workspace picker。测试 `mode` 与草稿跨重新创建 store 恢复，选择只聊天时没有 HTTP 或 session 调用，侧边栏新建对话不清空或转移草稿。
6. 实现 composer、创建状态机和 handoff。用受控 sessions/remote/input doubles 断言一次预留、一次 create、一次 activate、预设事件先到、三秒先到、晚到事件、错误释放资源和唯一 `submit()`。
7. 实现侧边栏。测试 records 分类、归档和子 agent 排除、创建时间排序、手工顺序覆盖活动排序、标题/内容搜索隔离、取消过时搜索和失效 record 行为。
8. 运行 `pnpm run test`、`pnpm run typecheck`、`pnpm run build` 和打包 smoke test。每次失败先修复根因，再重跑受影响的最窄命令。
9. 通过仓库 `pnpm run verify:isolated-profile` 创建并验证隔离 profile；真实浏览器验证只能使用 `pnpm run start:isolated-web`。不得在当前 shell 手工设置 `DSH_HOME` 或 `DSH_AGENTS_HOME`，不得直接调用 `dsh`，不得读取、修改或复用用户正在使用的 profile。
10. 实现结束后只运行与改动直接相关的窄验证和仓库隔离入口；不运行完整宿主测试、不启动非隔离 Web、不执行递归临时目录清理。

### 预设处理约束

这里的“预设”是新建页顶部的 agent preset 选择，例如用户把下一段对话切换到某个自定义预设。官方组件把这个选择暂存于它自己的浏览器内存中，只有当一个新的空白会话已经成为当前会话时，才异步把该选择写入该会话。

`dsh-just-chat` 的流程恰好不同：它创建目录和会话后，需要等待标准新建会话流程完成预设处理，再交接首条草稿。当前 rc.2 没有公开方法让插件取得“用户刚刚在预设控件里选了哪一个”，也没有公开完成通知让插件可以在发送前等待该选择写入会话。

已核验的轮询方案不能解决预设存在时的判断问题。部署完全没有预设时，会话状态的 `agentPreset` 保持缺失，插件可以在会话创建后直接交接首条消息，因为不存在要等待的预设选择。

部署存在预设时，插件可以从会话状态读到当前 `agentPreset`，并监听 `agent-preset/selected` 事件；但新会话创建成功时就已经带着默认预设，`agentPreset` 已是有效值。只有用户暂存了不同预设且宿主完成重组时，才会出现后续事件和状态变化。插件不知道用户是否暂存了选择或目标 ID，因此无法分辨“默认预设已就绪，可以发送”和“还在等待用户选择的预设”。等待一个事件会使默认预设场景永久等待；固定三秒等待可能在预设切换前发送，这是已确认接受的行为。

这不影响模型选择：模型目录是会话创建后才加载的会话级能力，仍由官方控件处理。问题只存在于“在创建会话前选择 agent preset，然后自动发送”的这一条时序。

可选处理如下：

用户已确定采用固定延迟规则，时长为三秒：插件在创建会话前监听该会话的 `agent-preset/selected`，事件到达后立即交接草稿；没有事件时，自会话创建成功起固定等待三秒后交接草稿。该规则不是预设结算确认：网络慢、重组失败或超过三秒时，首条消息会使用届时会话实际采用的预设，通常是默认预设。

不能把发送任务可靠地排到官方预设任务之后：预设控制器的暂存状态、异步任务和完成 Promise 都是其私有实现，插件既不能取得也不能依赖跨插件监听器顺序。`InputActions.submit()` 也是异步入口，但它只启动自己的输入投递流程，不会与预设控制器形成顺序关系。

## 测试与验证

### 宿主测试

- 根目录未配置、无效、权限不足与设置变更。
- 模板参数、非法表达式、反斜杠、路径穿越、保留名、超长路径、空目录名，以及拒绝 `${cwd}` 和其他绝对路径表达。
- 默认模板、嵌套目录、中文首句、英文单词、空文本拒绝与同秒目录冲突。
- 目录创建、会话创建、分类记录、恢复标记和首条消息交接的成功路径。
- 每个失败点都保留正确的目录、会话、记录和草稿状态。

### 客户端测试

- 选择“不在项目中工作”只改变待选模式，不发生目录或会话调用。
- 根目录未配置时可编辑草稿、提交后打开设置并保留草稿。
- 选择“不在项目中工作”并输入文本后，刷新或重启独立浏览器实例，确认待选模式和未发送文本恢复，且仍没有目录或会话。
- 在未发送文本时点击侧边栏“新建对话”，确认持久化草稿未被清空；若宿主按当前或最近工作区打开普通空白会话，确认该草稿不自动写入普通会话。
- 无预设部署创建会话后直接交接首条消息；存在预设时验证 `agent-preset/selected` 先到即交接、创建成功后三秒届满即交接、事件晚到不重复发送，且成功后只发送一条消息。
- 切换普通工作区与“不在项目中工作”时文本草稿正确交接。
- 对话列表的过滤、归档、子 agent 排除、创建时间初始排序、拖动后持久化顺序、稳定并列排序和失效记录。
- 对话独立搜索的标题命中、内容命中、加载、取消、清空、无结果和会话打开。

### 端到端验证

- 验证必须复现普通用户路径：只能执行仓库 `pnpm run verify:isolated-profile`；需要浏览器时只能执行 `pnpm run start:isolated-web`，由脚本创建并向子进程传递隔离环境。不得预装 `@deepseek-ai/dsh-web-app` 或其他会替插件补充依赖的包；不得读取、修改或复用用户正在使用的 `web` profile、其设置、会话或工作区。
- 使用浏览器工具打开该确切 URL，而不是当前 `http://127.0.0.1:3080`。
- 在独立实例配置根目录与默认模板，选择“不在项目中工作”后确认磁盘无新目录、会话列表无新会话。
- 输入并发送首条消息，确认目录路径、会话 `cwd`、首次消息和“对话”列表归属。
- 刷新独立实例后确认会话持续存在，追加消息不改变创建时间排序；验证拖动后顺序持久化。
- 验证模板嵌套目录、中文路径、空格路径、无效模板、目录权限失败、消息搜索和普通工作区不受影响。
- 只有实际插件仓库中 `pnpm run dev:web` 正在运行时才可验证客户端热更新；否则重建对应 Web 产物并刷新既有 GUI，不能另起替代 Web 服务。

### 隔离验证与清理安全

DSH 安装、profile 初始化和浏览器验证只能通过仓库脚本创建的隔离环境执行，不能在当前 shell 中手工设置 `DSH_HOME` 或 `DSH_AGENTS_HOME` 后直接调用 `dsh`。每次验证由脚本在系统临时目录下创建唯一 `sandboxRoot`，并分别使用其 `dsh-home` 与 `agents-home` 子目录作为子进程专用的 `DSH_HOME` 与 `DSH_AGENTS_HOME`；父进程环境不能被修改。

隔离环境必须同时满足以下不变量：

- `sandboxRoot` 是规范化系统临时目录的直接子目录，名称具有 `dsh-just-chat-isolation-` 固定前缀，且不是符号链接或目录联接点。
- `dsh-home` 与 `agents-home` 都是 `sandboxRoot` 的严格后代；profile 清单必须位于 `dsh-home/profiles/<profile>/package.json`。
- 创建 `sandboxRoot` 时写入带随机所有权编号、规范化绝对路径和固定用途的标记；任何清理都必须重新核对该标记。
- 删除目标只能是通过所有权校验的 `sandboxRoot`，不能来自 `DSH_HOME`、`DSH_AGENTS_HOME`、`USERPROFILE`、`HOME`、工作区路径、通配符或 shell 环境变量。

验证默认保留隔离目录并输出规范化绝对路径和所有权编号；失败时同样保留，便于诊断。清理是单独的显式命令，执行前必须向用户展示规范化绝对目标和所有权编号并取得确认。清理入口在递归删除前重新验证临时目录父级、固定前缀、目录类型、所有权标记、标记路径和所有权编号；任一不一致都拒绝执行，不提供替代删除路径，也不使用 `force: true`。

实现顺序固定为：先集中实现隔离目录创建、验证和受控清理；再改造 profile 验证脚本只向子进程传递隔离环境并取消 `finally` 自动删除；随后增加只覆盖危险路径拒绝、所有权标记、子进程环境隔离和显式清理授权的最小测试。隔离验证和清理必须在默认沙箱内运行，不得为了访问用户目录申请提升权限。

## 后续迭代

- 由用户明确触发的“删除会话并处理目录”能力，届时需单独定义运行中会话、二次确认、回收站、目录非空和会话日志保留规则。
- 自动目录的保留期限、存储用量与批量清理。
- 将已有会话转换为自动对话或普通工作区会话。
- 目录模板历史、导入导出与跨设备同步。

## 审查记录

原审查基于官方 `dsh-v0.1.0-rc.7`；更新到 `dsh-v0.1.1-rc.2` 后又针对设置导航和无会话输入契约复核变化。事务恢复、官方接口时序、客户端替换、打包与独立验证的结论如下。

- P0：无。`storage-domain` 不提供跨目录和会话创建的原子事务，但本流程不要求它提供该能力：目录和 `prepared` 记录先持久化；会话存在时幂等激活，缺失时不发布活动会话。官方对已存在目录的递归创建是幂等操作，不构成目录冲突。外部插件应依赖独立 DSH 子包，不依赖根包 `@deepseek-ai/dsh`；profile 的依赖闭包能够解析这些子包。
- P0（已由 2026-09-01 清单修正覆盖）：普通 `dsh web` 的 profile 不会把全局 CLI 自己的 `node_modules` 暴露给插件。插件宿主产物会实际加载 `dsh-storage-domain`、`dsh-settings` 和 `schemastery`，所以这些包及其 `dsh-storage`、`dsh-invariants`、`dsh-brand` 必须作为插件的生产依赖安装到 profile；`@deepseek-ai/cordis` 是共享宿主 peer，不再作为本插件生产依赖。`dsh-storage-json` 未被本插件实际导入，不声明为发布依赖。客户端通过 DSH Web 的模块加载器取得 React 和 `dsh.client.inject` 中声明的宿主服务，继续作为兼容性 peer，而不在插件中复制一套客户端运行时。
- P0（历史验证边界）：隔离 Web profile 只是验证依赖解析的测试夹具，不是正式用户场景；正式场景只有 `dsh plugin --profile web add <插件来源>`。隔离启动必须真正等待 HTTP 服务可访问后才算通过；仅 `--dump-config` 不能验证插件加载。
- P0：无。外部 slot injected face 必须用 `type` alias 而非 `interface`，以满足 registry 所需的隐式索引签名；`WorkspacePicker` 已用公开 `ctx.slots.inject/register`、`ctx.workspaces.create` 和 directory-flow child slot 完成类型检查验证。`PropsStore` actions 仍仅承担状态变换，业务回调保留在 injected face。
- P0：无。`conversation.composer.bar` 在 rc.2 的公开 slot declaration 中存在，作用域为 `session-maybe`；它是无 session 输入栏与首条消息延迟创建所需的合法挂载点。此前全包搜索漏报该定义，不能作为实现依据。
- P1：预设事件监听必须在客户端根作用域通过 `ctx.remote.$on('agent-preset/selected', ...)` 注册，并按预分配 `sessionId` 过滤。不能在会话创建前通过会话作用域监听，因为该作用域尚未存在。该修正没有产品取舍，实施时必须采用。
- P1：`prepared` 记录跨进程恢复的终态已确定。进程在创建会话和激活分类记录之间退出后，启动时按 `sessionId` 和 `cwd` 查询真实会话；存在则激活，不存在则删除创建中记录并保留空目录，不展示失败条目。
- 已撤回的错误结论：此前把“其他控件保持宿主无会话状态原有规则”误读为“无会话时也必须让会话级控件完整可用”，由此错误认定必须提前创建会话或修改 DSH 上游。实际规格只要求在用户选择只聊天后增加纯文本编辑与发送；模型、预设、附件、命令和计划在无会话时继续保持宿主原有的锁定或不存在状态，首条文本交接后恢复完整官方输入栏即可。
- P0：当前插件以 `priority: -1` 永久覆盖 `conversation.composer.bar`，并把宿主无会话 `disabled` 直接算入插件输入栏禁用状态，导致选择只聊天后仍无法输入，真实会话建立后也不能恢复官方输入栏。处理方式固定为上述“输入栏接管与退出修正计划”，不修改 DSH 上游，不提前创建目录或会话。
- 不成立：侧边栏替换、标题和内容独立搜索、三秒预设规则、重复 `mkdir`、独立 profile 启动端口、外部 DSH 子包依赖都已按官方源码核验，不属于 P0/P1。

## 进度跟踪

### 官方插件配置卡片迁移计划（2026-08-27）

- [x] 将客户端设置注册从 `settings.section` 迁移到 `settings.plugin.item`，以 `dsh-just-chat` 设置命名空间作为 keyed slot 的 `key`，并声明对 `@deepseek-ai/dsh-client-ui-settings-plugins` 的精确版本依赖和客户端注入依赖。
- [x] 保留现有 `SettingsScope`、目录浏览、预览、保存、错误处理和缺少根目录引导逻辑；只把设置组件的 runtime props 改为官方插件卡片槽位契约。
- [x] 使用公开 DSH primitives 重做卡片内容布局：根目录控件和模板控件各自独占一行，根目录行保留浏览按钮。
- [x] 在目录名模板输入框下直接列出全部可用参数、参数含义、默认模板和只读预览，确保模板可以在页面内完成使用。
- [x] 只执行本次迁移所需的类型检查、构建、打包和包 smoke 验证；不修改 DSH 上游，不删除隔离目录，不增加无关测试或审查。

- [x] 设置迁移完成：`settings.plugin.item` 已按 `dsh-just-chat` 命名空间注册，缺少根目录时改由 `openSection('plugins')` 打开官方插件设置分区；卡片使用公开 DSH primitives，根目录和模板各占独立一行，模板下方展示全部可用参数及含义。`pnpm run typecheck`、`pnpm run build`、`pnpm run pack`、`pnpm run smoke:package` 和设置引导的 2 条直接测试均通过。

- [x] 复现并定位普通 `dsh plugin --profile web add link:...` 后启动失败：插件运行时从 `dsh-storage-domain` 解析不到 `@deepseek-ai/dsh-storage`，根因是发布依赖被错误声明为 peer，且先前隔离 Web 环境额外安装了会掩盖缺包的 `dsh-web-app`。
- [x] 将宿主运行时依赖闭包改为生产依赖；链接安装已在 `<隔离临时目录>`（所有权编号 `<本轮所有权编号>`）的空 `web` profile 中启动成功。重新打包后的 tgz 已在 `<隔离临时目录>`（所有权编号 `<本轮所有权编号>`）启动，并返回 HTTP `200`；两处隔离目录均按规则保留，未执行清理。
- [x] 经用户确认后删除并重建 `<仓库本地依赖目录>`；新的依赖树已包含宿主运行时闭包，生成的本地 `.pnpm-store/` 已加入 Git 忽略规则。
- [x] 重新核验设置分区、工作区浏览器搜索、新建页工作区选择器和无工作区输入栏的现有扩展约束。
- [x] 记录用户确认的根目录、目录模板、延迟创建、排序、独立搜索、无删除和无转换决定。
- [x] 完成目录模板语法、路径分隔、移除 `${cwd}` 参数、输入交接、错误状态和边界场景设计。
- [x] 审查规格：已把“选择即创建”调整为“首条消息提交时创建”，并根据 rc.7 源码确认了三个单一占位。
- [x] 初始化本地 Git 仓库和私有远端 `favtony/dsh-just-chat`；当前没有提交或推送。
- [x] 曾以官方 `dsh-v0.1.0-rc.7` / `99f6f02` 源码核验实际模块、槽位、会话创建和输入交接接口；该源码参考子模块现已移除。
- [x] 获取官方 `dsh-v0.1.1-rc.2` / `b150a551b8` 时确认 `master` 与该标签相同。复核确认公开 `settings.onboarding.openSection` 可解决缺根目录设置导航；默认输入栏仍没有无会话提交扩展点，但这不阻塞插件临时提供首条纯文本输入。
- [x] 写入按仓库结构、宿主接口、事务、替换组件、测试和 CI 拆解的实施计划。
- [x] 完成实施计划 P0 审查：预留目录与会话 ID、标准输入交接、存储域和单一占位替换均无 P0。
- [x] 确定首条消息的预设处理：预设事件先到即发送，否则会话创建成功后三秒发送。
- [x] 确定未发送只聊天文本的生命周期：浏览器关闭、重新打开、刷新或点击侧边栏“新建对话”后恢复待选模式和纯文本草稿；草稿写入真实会话输入状态机后清除本地副本，侧边栏“新建对话”不清空也不自动转移草稿。
- [x] 确定中断创建恢复：会话不存在时删除 `prepared` 或 `active` 记录、保留空目录且不显示条目；会话存在且目录一致时激活或保留记录。恢复逻辑不创建错误记录、不提供失败条目。
- [x] 创建最小双端插件脚手架：根清单、TypeScript/tsdown/Vitest、CI 和打包 smoke 脚本已落盘；依赖安装及 `pnpm run typecheck` 已通过。
- [x] 撤回旧的完整替换实现：`ui-workspace` 默认 row 保持启用，插件不重新声明三个同名 slot，也不重新实现目录选择 child slot；客户端改为读取、严格校验并包装当前官方 `conversation.hero.workspace`、`conversation.composer.bar`、`sidebar.workspaces` live entry，卸载时恢复原始 component。
- [x] 修正发布 manifest 与实际 ESM 构建产物：根与 client exports 指向 `.mjs`，发布包只包含构建产物、patch 和 README；`pnpm run pack` 与 `pnpm run smoke:package` 均通过。
- [x] 实现宿主半部，并覆盖模板、路径、预留、恢复和路由。
- [x] 实现客户端半部：workspace picker、composer、交接、sidebar、settings、独立草稿和排序持久化。
- [x] 执行 `pnpm run typecheck`、`pnpm run test`（30 项）、`pnpm run build`、`pnpm run pack`、`pnpm run smoke:package` 和 `pnpm run verify:isolated-profile`。
- [x] 实现隔离验证与受控清理防御：独立 `sandboxRoot`、所有权标记、固定 `dsh-home`/`agents-home`、子进程专用环境、默认保留和显式确认清理。
- [x] 完成 13 项纯逻辑安全断言、脚本语法检查和项目类型检查；真实隔离 profile 验证通过并保留 `<隔离临时目录>`，所有权编号为 `<本轮所有权编号>`。本轮没有执行清理，也没有扫描、修改或清理用户目录。
- [x] 修复客户端提交失败后未释放本次运行状态的问题；预留、会话创建或激活失败后保留草稿并可重新提交，交接只在 `setDraft` 与 `submit` 均同步接受后清除本地草稿。
- [x] 普通工作区选择使用独立 `workspace` 待交接模式，只在用户从工作区选择器明确改选普通工作区时把草稿写入官方输入状态；侧边栏打开或新建会话不转移该草稿。
- [x] 根目录和模板改由 `PUT /api/dsh-just-chat/settings` 在宿主一次 `settings.update` 中原子保存；自动对话行已补入运行、待处理、完成提醒和重命名、分叉、归档动作。
- [x] 上述修复通过 29 条直接相关测试、`pnpm run typecheck`、`pnpm run build` 与 `pnpm run smoke:package`；没有运行含递归临时目录清理的完整测试。
- [x] 撤回“必须扩展 DSH 上游或提前创建会话”的错误结论；确认现有宿主与客户端代码已经支持“点击发送后创建目录和会话，再通过 `InputActions` 交接首条文本”。
- [x] 处理 composer P0：只在明确待选模式且无会话时注册临时输入栏；只聊天模式忽略宿主的工作区选择禁用，目录和会话仍在点击发送后创建；会话出现后保留覆盖直到普通工作区草稿交接或只聊天首条提交同步接受，随后撤销覆盖并恢复官方输入栏。
- [x] 补齐预设事件或三秒计时晚于会话出现时的明确交接触发，避免组件因 props 未变化而漏发首条消息；新增注册生命周期、启用条件和交接时序共 4 条直接测试，与既有 7 条 handoff 测试一起通过，项目类型检查和构建通过。
- [x] 确认 `external/deepseek-harness` 不在 pnpm workspace、TypeScript include、构建入口、Vitest 配置或发布文件中；经用户确认后已移除子模块工作目录、`.gitmodules`、内部 Git 元数据、索引项和本地配置段。仓库以后只依赖正式发布的 DSH npm 包。
- [x] 缺少根目录的提交返回 `settings-missing-root` 后，客户端保留草稿并按请求临时注册 `settings.onboarding`，通过公开 `openSection('plugins')` 打开官方插件设置分区，完成请求后立即撤销引导项；启动和普通无会话状态不会自动打开设置。相关 13 条测试与项目类型检查通过。
- [x] 完成独立 `3188` profile 的真实浏览器交互验证：选择“不在项目中工作”后目录和会话均未创建；草稿刷新后恢复；缺根目录的提交打开“对话目录”并保留草稿；保存后再次提交才创建严格后代目录和活动会话；投递记录为 1 轮，交接后恢复官方输入栏。隔离实例未配置模型密钥，因此消息投递后模型按官方行为报告 `MISSING_CREDENTIAL`，不影响目录、会话和首条消息交接结论。
- [x] 真实页面发现并修复完整替换遗漏 `sidebar.workspaces.directoryFlow` 导致“添加工作区”消失的问题；新隔离实例使用官方目录浏览器添加 `workspace-root` 后正常创建普通工作区和空白会话，官方输入控件完整出现，且该会话没有进入插件“对话”列表。最终隔离目录 `<隔离临时目录>` 和所有权编号 `<本轮所有权编号>` 已保留，未执行清理。
- [x] 创建 `.gitignore` 排除 `node_modules/`、`.artifacts/` 和 `.agent-teams/`；源码、必要测试、CI、发布构建产物和文档已显式纳入 `feature/just-chat` 的初始本地提交，不包含 DSH 子模块记录，未推送远端。
- [x] 增加宿主版本硬校验：安装前脚本和宿主入口均要求 `@deepseek-ai/dsh-client-runtime`、`@deepseek-ai/dsh-client-ui-renderer`、`@deepseek-ai/dsh-client-ui-workspace`、`@deepseek-ai/dsh-client-ui-conversation` 精确为 `0.1.1-rc.2`；`--ignore-scripts` 只能跳过安装阶段，不能绕过启动阶段拒绝。`ui-slots` 仍用于类型和注入边界，但不是当前 DSH 安装中的独立运行时包，因此不作为版本检查目标。
- [x] 当前窄验证通过：`tsc --noEmit`、构建声明、`tsdown`、`scripts/check-host-compat.mjs`、`pnpm run pack`、`pnpm run smoke:package` 和 `pnpm run verify:isolated-profile` 均成功；最新隔离目录 `<隔离临时目录>`、所有权编号 `<本轮所有权编号>` 已保留，未执行清理。
- [x] 记录官方 `WorkspaceBrowser` 公开接口的已知限制：消息内容搜索已按插件自动会话过滤；官方组件把标题本地搜索直接作用于完整 `useSessions` 列表，公开 props 没有搜索查询或搜索结果渲染边界，因此当前无法在不复制官方搜索界面的前提下阻止普通工作区会话因标题命中出现在“对话”搜索结果中。该限制保留在 P1，不引入第二套浏览器或 fallback。
- [x] 最终隔离 profile 验证仍通过；最新目录 `<隔离临时目录>`、所有权编号 `<本轮所有权编号>` 已保留，未执行清理。
- [x] 复核用户反馈的“界面没有变化”：按仓库规定启动全新 `pnpm run start:isolated-web` 实例，实际下发的 `__DSH_BOOT__` 包含 `/plugins/dsh-just-chat/client.js`，浏览器页面的官方工作区菜单实际显示“`不在项目中工作`”和“`添加工作区…`”；用户截图对应的 `3080` 当前没有监听进程，不能作为当前客户端入口未加载的证据。未修改 DSH 上游、未删除任何隔离目录。
- [x] 在已有普通工作区和空白会话中复现“选择`不在项目中工作`后顶部仍显示真实工作区”：根因是 `ConversationRoot` 在 `conversation` live entry 外层读取宿主工作区快照，原插件只投影了 `conversation.hero.workspace` 选择器内部的列表；修复计划是继续复用官方 `conversation` entry，投影只聊天条目并拦截伪 ID 的官方切换回调，不复制或重画任何官方控件。
- [x] 修复 live entry 适配器破坏官方 Hook 契约的问题：`useWorkspaces`、`useSessions`、`useStore`、`useDirectoryFlow`、`useHostDescription` 和 `useInput` 现在在官方组件调用时继续执行原始订阅 Hook，只替换其读取的投影快照；修复前真实页面出现 `InputBar` 的 `undefined.length`，修复后全新页面无该错误。
- [x] 在全新隔离 Web 实例中复现并修复“普通工作区先自动创建空白会话，再切换只聊天”的路径：只聊天判断现在识别宿主空白会话，但不接管已由插件创建的会话。隔离目录 `<隔离临时目录>`，所有权编号 `<本轮所有权编号>`，均已保留未清理；实际点击后顶部在 `workspace-root` 与“`不在项目中工作`”之间正确切换，发送首条文本只在 `<测试对话目录>` 创建目录和独立会话，普通工作区仍保留自己的空白会话，会话进入“对话”分区。官方随后按无 API key 报告 `MISSING_CREDENTIAL`，目录、cwd、分类和首条消息交接均已验证。
- [x] 最终重新打包并执行 `pnpm run verify:isolated-profile`；空 `web` profile 安装和 Web 启动通过，隔离目录 `<隔离临时目录>`、所有权编号 `<本轮所有权编号>` 已保留，未执行清理。
- [x] 修正设置页的原生行布局：右侧根目录控件收敛到 DSH 设置内容区的原生可用宽度，左侧标题和说明不再被压成竖排；重新构建后在隔离页面确认 DSH 原生设置外壳、输入框、浏览/预览/保存控件均正常显示。
- [x] 修正隔离 Web 启动入口：不再强制注入 `browse` 目录选择器补丁，恢复 DSH 默认的目录选择器组合，使 Windows 隔离实例中的 `ctx.workspaces.pickDirectory()` 使用 `native` `host.pickDirectory` 流程；此前的 browse 补丁文件保留为历史验证资产但不再由默认启动入口加载。
- [x] 用户确认侧栏“对话”提升为与“工作区”同层；已确定使用两个官方 `WorkspaceBrowser` 实例，工作区实例固定 workspace 分组、对话实例固定 flat 分组，保留第二个官方实例的搜索框和视图菜单，不复制官方行组件、不修改 DSH 上游。
- [x] 实现双官方 `WorkspaceBrowser` 侧栏组合：移除虚拟 workspace 投影，工作区和对话使用各自的数据投影；对话实例固定官方 flat 模式并接入独立持久化顺序。工作区实例过滤官方 flat 顺序回写，避免普通工作区空白会话污染插件顺序；类型检查、构建、打包、包 smoke 和隔离 profile 验证通过。浏览器扩展控制端本轮未连接，未新增页面操作结论；此前隔离 Web 页面验证记录仍有效。
- [x] 完成双实例实现后的最终构建、打包、包 smoke 和隔离 profile 验证；最新隔离目录 `<隔离临时目录>`、所有权编号 `<本轮所有权编号>` 已保留，未执行清理。
- [x] 左侧“工作区”和“对话”已改为共享外层滚动区：侧栏适配器外层承接纵向滚动，两个官方浏览器分区按内容高度连续排列；未修改官方组件、数据投影或交互契约。`pnpm run typecheck`、`pnpm run build`、`pnpm run pack` 与 `pnpm run smoke:package` 已通过。
- [x] 尝试通过唯一允许的 `pnpm run start:isolated-web` 进行真实页面验证，但隔离脚本在安装插件前创建 DSH 子进程时受到运行环境限制，报 `spawnSync ... node.exe EPERM`。未申请提升权限，隔离目录 `<隔离临时目录>`（所有权编号 `<本轮所有权编号>`）已保留，未执行清理；共享滚动的页面级验证仍需在允许创建子进程的隔离环境中完成。
- [x] 实现复核：源码与生成客户端产物一致，改动仅涉及侧栏外层和两个分区的 flex/overflow 布局；没有发现 P0，亦没有规格偏离或额外实现。

### Cordis 共享宿主依赖清单修正（2026-09-01）

#### 已确定的目标与范围

- 正式安装入口只有 `dsh plugin --profile web add <插件来源>`；隔离 profile 仅是验证夹具，不作为用户场景或清单设计前提。
- 处理 `dsh-market` 对 `dsh-just-chat` 的静态提示：`@deepseek-ai/cordis` 被声明在 `dependencies`，而市场将其列为 DSH 共享宿主包。
- `@deepseek-ai/cordis` 从插件生产依赖移到 `peerDependencies`，并保留在 `devDependencies` 供类型检查和构建使用；`dsh-settings`、`dsh-storage-domain`、`dsh-storage` 等确需随插件安装的生产依赖保持不变。
- 不修改 DSH 上游、不修改 `dsh-market`、不改变插件运行逻辑，不引入任何 fallback。

#### 验证标准

- `package.json` 与锁文件不再把 `@deepseek-ai/cordis` 作为 `dsh-just-chat` 的生产依赖；插件仍声明宿主 peer。
- 类型检查、单元测试、构建、打包和包 smoke test 通过。
- 通过正式 `dsh plugin --profile web add` 安装更新后的 GitHub 版本后，profile 能启动插件；`dsh-market` 不再对本插件生成 `shared-host-package-dependency` 诊断。

#### 进度

- [x] 修改依赖清单并更新锁文件；`@deepseek-ai/cordis` 已从生产依赖移到宿主 peer，并保留在开发依赖。
- [x] 执行类型检查、51 项单元测试、构建、打包和包 smoke test。
- [x] 用正式 `dsh plugin --profile web add <本地 tgz>` 更新当前 web profile；安装成功，`dsh-just-chat` 不再携带 Cordis 生产依赖，市场诊断函数返回空 `findings`。
- [x] 已将修正版提交 `7f44dbfa7d10494e581859e85b28dffe2229e5bc` 推送到 GitHub，并通过正式 `dsh plugin --profile web add github:favtony/dsh-just-chat#7f44dbf` 将当前 web profile 切换到 GitHub 来源；安装成功，`dsh-just-chat` 清单中的 Cordis 共享宿主依赖诊断返回空 `findings`。
